#include <bits/stdc++.h>
  using namespace std;
  
  int main()
  {
    int t;
    cin>>t;
    while(t--)
    {
      int n;
      cin>>n;
      int arr[n];
      for(int i=0;i<n;i++)
      cin>>arr[i];
      int right;
      right=arr[n-1];
      cout<<right<<" ";
      for(int i=n-2;i>=0;i--)
      {
        if(arr[i]>=right){
        right=arr[i];
        cout<<right<<" ";
        }
      }
      cout<<"\n ";
    }
    return 0;
  }
    
    
